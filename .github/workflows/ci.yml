name: CI

on:
  pull_request:
  push:
    branches: [main]

# A new push to a PR makes the in-flight run irrelevant. Keep main serialized.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read

jobs:
  lint:
    name: Lint
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Check formatting
        run: swift format lint --recursive --strict Sources Tests

      - name: Check shell scripts
        run: |
          command -v shellcheck >/dev/null || brew install shellcheck
          shellcheck Scripts/*.sh

  build:
    name: Build & test
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      # FluidAudio pulls a prebuilt xcframework and compiles ~250 files; without
      # a cache every run pays ~3 minutes for dependencies that rarely change.
      - name: Cache SwiftPM
        uses: actions/cache@v4
        with:
          path: |
            .build
            ~/.cache/org.swift.swiftpm
          key: spm-${{ runner.os }}-${{ hashFiles('Package.resolved') }}
          restore-keys: spm-${{ runner.os }}-

      - name: Build
        run: swift build --configuration release

      - name: Test
        run: swift test --parallel
